export type GuestFormData = {
  name: string;
  rg: string;
  cpf: string;
  phoneNumber: string;
  birthDate: string;
};

export type GuestData = {
  id: number;
  name: string;
  rg: string;
  cpf: string;
  phoneNumber: string;
  birthDate: string;
};
